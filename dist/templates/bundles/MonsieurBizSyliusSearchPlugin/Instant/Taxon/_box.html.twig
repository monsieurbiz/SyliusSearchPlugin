{% import "@SyliusShop/Common/Macro/money.html.twig" as money %}

<div class="result">
    <div class="content">
        <a href="{{ path('monsieurbiz_sylius_search_taxon', {'slug': item.slug, '_locale': sylius.localeCode}) }}" class="header sylius-taxon-name">{{ item.name }}</a>
    </div>
</div>
